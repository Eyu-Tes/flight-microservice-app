server:
  port: 8001

spring:
  application:
    name: FlightRTDIS
  config:
    import: optional:configserver:http://localhost:8888

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
